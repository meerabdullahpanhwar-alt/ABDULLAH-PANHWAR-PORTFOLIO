// Example: Simple console log to confirm script is linked
console.log("Portfolio script loaded!");

// You might add code here for:
// 1. Smooth scrolling between sections.
// 2. A light/dark mode toggle.
// 3. Form validation for the contact section (if you add a form).

/*
// Example for smooth scrolling (requires a polyfill for older browsers)
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();

        document.querySelector(this.getAttribute('href')).scrollIntoView({
            behavior: 'smooth'
        });
    });
});
*/